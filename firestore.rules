rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all authenticated users to read/write everything for development
    // This will be restricted in production
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
